<%= form_with(model: question, local: true) do |form| %>
  <% if question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :Chapter %>
 <%= form.collection_select :chapter_id, Chapter.all,:id,:title, include_blank: true %>
</div>
 <div class="field">
    <%= form.label :Question_category %>
    <%#= form.select :question_category, ['Easy', 'Medium', 'Hard'] %>
    <%= form.select :category, options_for_select([['Easy'], ['Medium'], ['Hard']], :selected => :category) %>


    <%#= form.select(:question_category, ['Easy', 'Medium', 'Hard'], {}, { :class => 'my_style_class' }) %>
 <%#= form.collection_select :question_category_id, QuestionCategory.all.uniq,:id,:title, include_blank: true %>
</div>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :question_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
