<%= form_with(model: question_category, local: true) do |form| %>
  <% if question_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_category.errors.count, "error") %> prohibited this question_category from being saved:</h2>

      <ul>
      <% question_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
    <%#= form.label :Chapter %>
 <%#= form.collection_select :chapter_id, Chapter.all,:id,:title, include_blank: true %>
</div>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :question_category_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
